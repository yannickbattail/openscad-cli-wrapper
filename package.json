{
  "name": "openscad-cli-wrapper",
  "description": "wrapper library around openscad",
  "version": "1.4.10",
  "author": "XcinnaY",
  "maintainer": "XcinnaY",
  "license": "LGPL-3.0-or-later",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/yannickbattail/openscad-cli-wrapper"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "scripts": {
    "clean": "rm -Rf ./*.js ./*/*.js coverage dist tests/unit/run/out || true",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "vitest run --coverage ./tests/",
    "lint": "eslint --fix src tests",
    "format": "prettier --log-level error --write . --ignore-path ./.gitignore",
    "depcheck": "depcheck --quiet --ignore-path=./.gitignore --ignores='@types/*,@vitest/coverage-v8'",
    "check": "npm run depcheck && npm run build && npm run lint && npm run format && npm run check-amer && npm run test && npm run test",
    "check-amer": "grep amersports package-lock.json || (echo 'ERROR: amersports found in package-lock.json' && exit 1)",
    "check-updates": "npm-check-updates -u && npm i && npm dedupe && npm run check",
    "publish-new-v": "npm version patch --no-git-tag-version && npm run check && npm publish"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "chalk": "^5.6.2",
    "nanoid": "^5.1.6"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^24.10.1",
    "@vitest/coverage-v8": "^4.0.10",
    "depcheck": "^1.4.7",
    "eslint": "^9.39.1",
    "globals": "^16.5.0",
    "npm-check-updates": "^19.1.2",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.47.0",
    "vitest": "^4.0.10"
  }
}
